{
    "signal": "SIGHUP",
    "env": {
        "NODE_ENV": "development"
    },
    "watch": [
        "src/"
    ],
    "ext": "ts, js, txt, json",
    "delay": 0,
    "exec": "tsc && node --inspect ./dist/server.js  || exit 1"
}